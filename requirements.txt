# Pose-IQ dependencies (Python 3.12)

# API
flask>=3.1
flask-cors>=5.0
gunicorn>=23.0   # production WSGI server (Render/Railway) — `flask run` is dev-only

# Database — MongoDB
pymongo>=4.10
python-dotenv>=1.0

# Computer vision / real-time pipeline
opencv-python==4.13.0.92
mediapipe==0.10.33
numpy==2.2.5

# Machine learning
scikit-learn==1.6.1
joblib==1.5.1

# Voice coaching (desktop pipeline)
pyttsx3>=2.99

# ── Dev / test ────────────────────────────────────────────
mongomock>=4.1
pytest>=8.0
