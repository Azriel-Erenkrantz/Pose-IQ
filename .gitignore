__pycache__/
*.pyc
*.task
.claude/
data/user_profile.json
data/workout_history.json

# ── Secrets ──────────────────────────────────────────────
.env
*.local

# ── Database (dev) — prod uses PostgreSQL via DATABASE_URL ─
data/poseiq.db
*.db
*.sqlite
*.sqlite3

# ── ML artifacts (regenerated by extractor/trainer) ──────
data/models/
*.joblib
data/video_data.csv
data/training_data.csv
data/*.parquet

# ── Training videos (large source data, ~191MB) ──────────
# Too big for git — keep in external/cloud storage (or git-lfs).
data/videos/
